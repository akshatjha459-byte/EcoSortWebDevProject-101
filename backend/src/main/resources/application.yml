app:
  name: EcoSort
  version: 0.1.0

server:
  port: 8080

spring:
  application:
    name: ${APP_NAME:ecosort-api}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
